/**
 * Trigger.dev Client Utilities
 * 
 * Note: This SDK wrapper is deprecated.
 * Use @trigger.dev/sdk/v3 directly instead.
 */

// Re-export the necessary functions from Trigger.dev SDK
export { tasks, runs } from '@trigger.dev/sdk/v3'

// Export types
export type { ExecutionRequest, ExecutionResult } from '@vibe-code/types'
