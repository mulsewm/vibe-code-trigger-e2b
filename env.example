# =============================================================================
# Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# Trigger.dev Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key from https://trigger.dev/
# 1. Sign up at https://trigger.dev
# 2. Create a new project or use existing one
# 3. Go to API Keys section
# 4. Copy your "Secret Key" (starts with "tr_dev_" or "tr_prod_")
TRIGGER_API_KEY=tr_dev_your_key_here
TRIGGER_SECRET_KEY=tr_dev_your_key_here

# Optional: Custom Trigger.dev API URL (usually not needed)
# TRIGGER_API_URL=https://api.trigger.dev

# -----------------------------------------------------------------------------
# e2b Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key from https://e2b.dev/
# 1. Sign up at https://e2b.dev
# 2. Go to Settings > API Keys
# 3. Create a new API key
# 4. Copy the key (starts with "e2b_")
E2B_API_KEY=e2b_your_key_here

# -----------------------------------------------------------------------------
# API Server Configuration
# -----------------------------------------------------------------------------
# Port for the API server (default: 3001)
PORT=3001

# Host for the API server (default: 0.0.0.0)
HOST=0.0.0.0

# API prefix for routes (default: /api)
API_PREFIX=/api

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
# URL of your API server
# - Local dev: http://localhost:3001
# - Production: https://your-api.vercel.app
NEXT_PUBLIC_API_URL=http://localhost:3001

# Internal API URL (used by server-side code)
API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Execution Configuration
# -----------------------------------------------------------------------------
# Default timeout for code execution (milliseconds)
EXECUTION_DEFAULT_TIMEOUT=30000

# Maximum allowed timeout (milliseconds)
EXECUTION_MAX_TIMEOUT=300000

# Polling interval for execution status (milliseconds)
EXECUTION_POLL_INTERVAL=500

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Enable CORS (true/false)
CORS_ENABLED=true

# Allowed CORS origin
# - Local dev: http://localhost:3000
# - Production: https://your-app.vercel.app
# - Multiple origins: separate with commas
CORS_ORIGIN=http://localhost:3000

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
NODE_ENV=development

